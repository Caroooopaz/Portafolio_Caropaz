pip --version #instalar librerías
pip list
pip install django #instalacion global
#conda en django

#crear entorno virtual (se asigna un espacio para trabajar con la estructura)
1. python -m venv nombre_entorno
#activar entorno
2. source capsulas/Scripts/activate
3. instalar django en el entorno virtual
pip install django  

#respaldar los packages de mi entorno
pip freeze > requirement.txt

#instalar dependencias
pip install -r requirement.txt

## crear proyecto django
django-admin startproject portafolio

#ejecutar proyecto
python manage.py runserver

#creando aplicación dentro del proyecto
python manage.py startapp proyectos

instalar postgresql
clave admin1234

quitar stack builder

#agregar aplicación
INSTALLED_APPS = [
    ...
    'proyectos',
]

#configurar bbdd
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "NAME": "mydatabase",
        "USER": "mydatabaseuser",
        "PASSWORD": "mypassword",
        "HOST": "127.0.0.1",
        "PORT": "5432",
    }
}

#agregar modulo
pip install psycopg2

#crear/actualizar tablas en bbdd
python manage.py migrate

#crear superusuario para admin
python manage.py createsuperuser

#crear modelo

(migrations => archivo que se convierte en tabla)

#crea un archivo que modificará una tabla
python manage.py makemigrations
python manage.py migrate

#registrar el modelo en admin.pý de la app

##tarea: mostrar todas las columnas en admin *lista*

para usar postgresql desde el cmd: psql -U caropaz: 

#########################
crear ruta
crear metodo
crear html

para variables de entorno python-dotenv
